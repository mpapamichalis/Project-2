<table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Blood Type</th>
                    <th scope="col">condition</th>
                </tr>
            </thead>
            <tbody>
                {{#each patients}}
                <tr>
                    <th scope="row">{{this.id}}</th>
                        <td>{{this.patientName}}</td>
                        <td>{{this.bloodType}}</td>
                        <td>{{this.patientCon}}</td>
                </tr>
                {{/each}}
          
              
            </tbody>
        </table>

        <button class="btn-add-donor" id="patadd-btn" type="submit"><strong>+</strong></button>
      

          <div class="handle-btns">
              <div><button class="d-circle" type="submit"> </a> 
        <a class="d-link" href="/donors">donors</a>
        </a></button></div>
         <div><button class="p-circle" type="submit"> </a> 
        <a class="p-link" href="/patients">patients</a>
        </a></button></div>
       
            <div><button class="b-circle" type="submit"> </a> 
        <a class="b-link" href="/blood">blood</a>
        </a></button></div>
        </div>